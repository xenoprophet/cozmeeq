{
  "name": "pulse-coreaudio",
  "version": "1.0.0",
  "private": true,
  "description": "N-API addon for CoreAudio aggregate device management",
  "main": "build/Release/pulse-coreaudio.node",
  "scripts": {
    "build": "cmake-js compile",
    "build:electron": "cmake-js compile --runtime=electron --runtime-version=$(node -e \"console.log(require('electron/package.json').version)\") --arch=$(node -e \"console.log(process.arch)\")",
    "rebuild": "cmake-js rebuild",
    "clean": "cmake-js clean"
  },
  "dependencies": {
    "node-addon-api": "^8.0.0"
  },
  "devDependencies": {
    "cmake-js": "^7.3.0"
  }
}
