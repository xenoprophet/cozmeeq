{
  "name": "@pulse/desktop",
  "version": "0.0.1",
  "private": true,
  "author": "Pulse",
  "license": "AGPL-3.0-only",
  "description": "Pulse Desktop — native Electron wrapper",
  "main": "dist/main.js",
  "scripts": {
    "dev": "node scripts/build.js && bunx electron .",
    "build": "node scripts/build.js",
    "build:native": "node scripts/build-native.js",
    "build:native:signed": "node scripts/build-native.js --sign",
    "forge": "bunx electron-forge",
    "package": "bun run build && bun run forge -- package && bash scripts/patch-macos.sh",
    "package:linux": "bun run build && bun run forge -- package --platform=linux --arch=x64",
    "package:win": "bun run build && bun run forge -- package --platform=win32 --arch=x64",
    "package:mac": "bun run build:native && bun run build && bun run forge -- package --platform=darwin --arch=arm64 && bash scripts/patch-macos.sh",
    "package:all": "bun run build && bun run forge -- package --platform=darwin --arch=arm64 && bash scripts/patch-macos.sh && bun run forge -- package --platform=linux --arch=x64 && bun run forge -- package --platform=win32 --arch=x64",
    "make": "bun run build && bun run forge -- make && bash scripts/patch-macos.sh"
  },
  "dependencies": {},
  "devDependencies": {
    "@electron-forge/cli": "^7.6.0",
    "@electron-forge/maker-dmg": "^7.6.0",
    "@electron-forge/maker-deb": "^7.6.0",
    "@electron-forge/maker-rpm": "^7.6.0",
    "@electron-forge/maker-squirrel": "^7.6.0",
    "@electron-forge/maker-zip": "^7.6.0",
    "@electron-forge/plugin-fuses": "^7.6.0",
    "@electron/fuses": "^1.8.0",
    "@types/node": "^22.0.0",
    "electron": "^40.4.1",
    "typescript": "^5.7.0"
  }
}
